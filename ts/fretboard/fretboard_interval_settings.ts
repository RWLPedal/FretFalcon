// Re-export shim — this file has moved to ts/schedule/fretboard_interval_settings.ts
// This shim exists only during Phase 3 and will be deleted once all callers are updated.
export { InstrumentIntervalSettings, InstrumentIntervalSettingsJSON } from '../schedule/fretboard_interval_settings';
